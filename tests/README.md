# Tests

This project currently relies on TypeScript and production QA scripts.

Future page work should add focused tests for:

- Domain calculations in `src/domain/trading.ts`.
- State transitions in `src/state/BrokerStore.tsx`.
- Form validation and permission behavior.
- QA script fixtures when delivery packages grow.

