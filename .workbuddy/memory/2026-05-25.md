# 2026-05-25

## 表单组件样式修改
- **去除 TextField.tsx 默认文字加粗**：删除了 `floatingLabel`、`input`、`selectText` 三处硬编码的 `fontWeight: '500'`，回归设计令牌 `fontWeight: '400'`
- 影响：TextField 标签文字、输入框文字、SelectField 选中值文字不再默认加粗

## Partner Tools 图标框架规范化
- **5 处硬编码图标 → AppIconFrame 组件**：Hero、Wallet、Performance、Distribution、SmallMetricCard 中的 `iconCircle`/`heroIconBadge` + 硬编码背景色 → `AppIconFrame` 组件
- 新增 `mapIconToneToBackgroundTone` 映射函数（amber→warning, blue→info 等）
- 删除废弃样式：`heroIconBadge`、`iconCircle`
- AppIconFrame 自动管理 `iconBg.*` 颜色和 icon tone，遵循设计系统规范

## Dev Console 底部固定
- **ProductControlPanel footerActions 移出 ScrollView**：Page navigation + Reset demo 按钮从 ScrollView 内部移到 panel 底部，始终固定
- panel 改为 `flexDirection: 'column'` + `overflow: 'hidden'`，ScrollView 添加 `flex: 1` 填充剩余空间
- footerActions 添加 `borderTopWidth` + `padding`，与 content 视觉分离

## Dev Console 图标颜色规范化
- **移除所有显式 tone 着色**：ScenarioTile、PageRow、ModuleAction、state summary 中的 `AppIconFrame` 和 reset 按钮的 `AppIcon` 全部删除 `tone` 属性，使用组件默认色
- 删除废弃代码：`scenarioToneKeys` 映射、`resolvePaletteIconTone` 函数
- 清理未使用 import：`IconTone`、`resolveThemeTone`、`ThemeColors`
- AppIconFrame 默认 `backgroundTone="neutral"` + 自动 icon tone="textDim"，符合组件规范

## 底部导航分割线调整
- **TabLayout.tsx** `tabBarStyle` 新增 `borderTopWidth: 0.5`，将默认约 1px 的 hairline 分割线改为 0.5px，更精致

## Discover 卡片间距规范化
- **DupoinDiscoverScreen widgetFlow 间距调整**：`gap: spacing.sm(8px)` → `columnGap: spacing.sm(8px)` + `rowGap: spacing.lg(16px)` + `paddingTop: spacing.md(12px)`
- 卡片行间距 16px 与 Screen 左右 padding 16px 一致，顶部 paddingTop 12px 补足 Screen 默认 4px 到 16px

## 图标大小规范化 — 符合设计系统 size.icon.* 令牌

**设计系统规则**：图标大小必须使用 `size.icon.*` 令牌（8/12/16/20/24/32/40/48/64），禁止任意数值。

### DiscoverModuleScreen.tsx (Profile 页面)
- Discover hero: `iconSize={22}` → `iconSize={20}`
- avatarEditBadge: `size={11}` → `size={12}`
- statCards (Deposit/Volume/New Verified): `size={42} iconSize={18}` → `sizeVariant="md" iconSize={16}`
- Manager card: `size={42} iconSize={22}` → `sizeVariant="md" iconSize={20}`
- ProfileListCard (Video Verified): `size={42} iconSize={22}` → `sizeVariant="md" iconSize={20}`
- ProfileCardHeader (Partner Portal): `size={42} iconSize={20}` → `sizeVariant="md" iconSize={20}`
- ProfileCardHeader chevron_right: `size={17}` → `size={16}`
- GiftIllustration: `size={72} iconSize={42}` → `sizeVariant="display" iconSizeVariant="xl"`

### PartnerToolsScreen.tsx
- HeroSignalPill: `size={14}` → `size={12}`
- FeatureTile: `size={28}` → `sizeVariant="lg"`（32px）

### DiscoverLayoutScreen.tsx
- 三处缩进/折叠图标: `size={13}`/`size={14}` → `size={12}`

### 共享组件
- ActionButton: `size={17}` → `size={16}`
- QuickActionSheet: `size={17}` → `size={16}`
