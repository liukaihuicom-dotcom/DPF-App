# Legacy Expo Template Files

This folder stores unused Expo template remnants that were moved out of the production source tree during directory cleanup.

The archive is excluded from TypeScript compilation in `tsconfig.json`.

