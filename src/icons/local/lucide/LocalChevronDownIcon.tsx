/* Vendored from lucide-react-native ChevronDown; source license: ISC. */
import { LucideIconBase } from '../LucideIconBase';
import type { LocalIconProps } from '../types';

const iconNode = [
  ["path", { d: "m6 9 6 6 6-6", key: "qrunsl" }]
] as const;

export function LocalChevronDownIcon(props: LocalIconProps) {
  return <LucideIconBase iconNode={iconNode} {...props} />;
}
