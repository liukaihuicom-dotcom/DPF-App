/* Vendored from lucide-react-native ChevronRight; source license: ISC. */
import { LucideIconBase } from '../LucideIconBase';
import type { LocalIconProps } from '../types';

const iconNode = [
  ["path", { d: "m9 18 6-6-6-6", key: "mthhwq" }]
] as const;

export function LocalChevronRightIcon(props: LocalIconProps) {
  return <LucideIconBase iconNode={iconNode} {...props} />;
}
