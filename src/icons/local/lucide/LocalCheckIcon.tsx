/* Vendored from lucide-react-native Check; source license: ISC. */
import { LucideIconBase } from '../LucideIconBase';
import type { LocalIconProps } from '../types';

const iconNode = [["path", { d: "M20 6 9 17l-5-5", key: "1gmf2c" }]] as const;

export function LocalCheckIcon(props: LocalIconProps) {
  return <LucideIconBase iconNode={iconNode} {...props} />;
}
