# Scripts

| Path | Purpose |
|---|---|
| `scripts/dev/` | Local development utilities, including the quote proxy |
| `scripts/qa/` | Executable production delivery checks |

Use package scripts rather than calling individual files directly where possible.

