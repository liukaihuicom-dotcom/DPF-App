# Token QA Checklist

- `tokens.json` and `token.schema.json` exist.
- Color source remains `tokens.color.json`.
- Runtime non-color tokens are mapped to `src/theme/tokens.ts`.
- `scripts/qa/check-hardcoded-style.js` passes.
