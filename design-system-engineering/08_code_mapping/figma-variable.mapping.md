# Figma Variable Mapping

| Source | Figma target |
|---|---|
| `tokens.color.json` | Color variables grouped by primitive and semantic mode |
| `token-mode.matrix.json` | Mode collection, currently `darkTerminal` |
| `component-manifest.json` | Component property documentation |
| `icon-registry.json` | Icon component names and semantic usage |
| `pattern-registry.json` | Pattern documentation frames |

Figma mapping is governed for handoff; this repository remains the code source of truth.
