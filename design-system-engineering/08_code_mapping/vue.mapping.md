# Vue Mapping

Vue is not an active runtime target for this Expo app.

If Vue delivery is introduced, it must consume the same token, component, icon, and pattern manifests and add a Vue-specific component mapping before release.
