# UI Build Binding

UI Build must consume:

- Product Kernel and Page Contracts from `product-engineering-package/`.
- Token, component, business component, icon, pattern, and platform manifests from `design-system-engineering/`.
- Copy and localization rules when user-facing financial copy is involved.
- QA scripts in `scripts/qa/` before handoff.
