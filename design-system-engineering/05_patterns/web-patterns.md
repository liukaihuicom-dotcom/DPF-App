# Web Patterns

Source of truth: `design-system/05-patterns` and `design-system/06-platform/web-rules.md`.

Web layouts may increase density but must keep the same contract states, permissions, and component registry usage as App/H5.
