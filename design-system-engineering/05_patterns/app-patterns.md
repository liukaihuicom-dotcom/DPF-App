# App Patterns

Source of truth: `design-system/05-patterns`.

App screens must use registered patterns from `pattern-registry.json`, route implementation boundaries from `component-usage-rules.md`, and platform behavior from `design-system/06-platform/app-rules.md`.
