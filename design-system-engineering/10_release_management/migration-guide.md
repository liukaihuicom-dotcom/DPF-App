# Migration Guide

## Current Migration

- Continue using `design-system/` for human-readable design documentation.
- Use `design-system-engineering/` for machine-readable governance, QA gates, mappings, and AI runtime inputs.
- Keep executable QA checks in root `scripts/qa/`.
- Add new visual QA artifacts under `qa/visual/`.
